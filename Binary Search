while(left<=right)
    {
       int mid=(left+right)/2;
        if(A[mid]==k)
            return mid;
        else if(k>A[mid])
            left=mid+1;
        else if(k<A[mid])
            right=mid-1;
        
